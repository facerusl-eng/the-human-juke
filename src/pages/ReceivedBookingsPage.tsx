function ReceivedBookingsPage() {
  return (
    <section className="create-gig-shell" aria-label="Received bookings">
      <section className="hero-card create-gig-card">
        <p className="eyebrow">Bookings</p>
        <h1>Received Booking Requests</h1>
        <p className="subcopy">
          New booking requests are currently delivered to your booking inbox email via the booking form.
        </p>
        <p className="subcopy">
          If you want, I can add an in-app bookings inbox next so you can review all requests directly in admin.
        </p>
      </section>
    </section>
  )
}

export default ReceivedBookingsPage
