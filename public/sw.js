/*
  Legacy service worker stub.
  Any browser that still has this URL registered will silently unregister it.
  The active service worker is now pwa-worker.js (generated by vite-plugin-pwa).
*/

self.addEventListener('install', () => {
  self.skipWaiting()
})

self.addEventListener('activate', (event) => {
  event.waitUntil(
    (async () => {
      try {
        const cacheKeys = await caches.keys()
        await Promise.all(cacheKeys.map((key) => caches.delete(key)))
      } catch {
        // Ignore cache cleanup issues.
      }

      await self.registration.unregister()
    })(),
  )
})

  )
})
